CREATE TABLE PLAYER (ID NUMBER(10) NOT NULL, DTYPE VARCHAR(31), P_NAME VARCHAR NOT NULL, PRIMARY KEY (ID))
CREATE TABLE BASEBALLPLAYER (ID NUMBER(10) NOT NULL, TOTALHOMERUNS NUMBER(10), PRIMARY KEY (ID))
CREATE TABLE PLAYERCARD (ID NUMBER(10) NOT NULL, DATE DATE, BACK_PIC VARCHAR, FRONT_PIC VARCHAR NOT NULL, ALBUM_ID NUMBER(10), PC_PLAYER NUMBER(10) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE ALBUM (ID NUMBER(10) NOT NULL, PRIMARY KEY (ID))
ALTER TABLE BASEBALLPLAYER ADD CONSTRAINT FK_BASEBALLPLAYER_ID FOREIGN KEY (ID) REFERENCES PLAYER (ID)
ALTER TABLE PLAYERCARD ADD CONSTRAINT FK_PLAYERCARD_ALBUM_ID FOREIGN KEY (ALBUM_ID) REFERENCES ALBUM (ID)
ALTER TABLE PLAYERCARD ADD CONSTRAINT FK_PLAYERCARD_PC_PLAYER FOREIGN KEY (PC_PLAYER) REFERENCES PLAYER (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
